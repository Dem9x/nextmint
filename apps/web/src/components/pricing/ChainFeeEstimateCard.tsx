export function ChainFeeEstimateCard() {
  return <div className="rounded-lg border border-white/10 bg-panel p-5 text-sm text-muted">Gas is estimated by your wallet on the selected testnet before signing.</div>;
}
