import { CatLoader } from "./CatLoader";
export function MintCatLoader() {
  return <CatLoader text="Cat is minting on-chain..." subtext="Waiting for wallet signature and blockchain confirmation." />;
}
